import React from 'react';
import styled from 'styled-components';

const DemoCardTagsContainer = styled.div`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
`;

export default DemoCardTagsContainer;
